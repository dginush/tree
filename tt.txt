// בדיקה 1: האם יש הרשאה
console.log('Permission:', Notification.permission);

// בדיקה 2: האם Service Worker רשום
navigator.serviceWorker.getRegistrations().then(r => console.log('SW:', r.length, r));

// בדיקה 3: נסה לקבל טוקן ידנית
PushNotifications.requestPermission();


Save token error: FirebaseError: Missing or insufficient permissions.
console.log('Permission:', Notification.permission);

VM5372:1 Permission: granted
undefined
navigator.serviceWorker.getRegistrations().then(r => console.log('SW:', r.length, r));

Promise {<pending>}
VM5377:1 SW: 1 [ServiceWorkerRegistration]
PushNotifications.requestPermission();
Promise {<pending>}
push-notifications.js:95 Save token error: FirebaseError: Missing or insufficient permissions.